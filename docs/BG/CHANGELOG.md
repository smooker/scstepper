# ИСТОРИЯ НА ПРОМЕНИТЕ — STM32F411CEU6 Stepper контролер

## [Непубликувано] — 2026-03-19 (v1.0 milestone)

### Добавено
- **Жест за хоминг от бутони**: при ES_L + задържан JOGL + натиснат JOGR → стартира RunHomeEx(1)
  - EVT_HOME флаг: ISR → main loop → RunHomeEx(fromButtons=1)
  - 300ms дебаунсиран abort при неедновременно пускане на бутоните
  - Abort проверка вградена в all polling loops (приближаване, утихване, backoff, паркиране)
  - 1s grace период + 150ms бип + prompt след успешен хоминг
  - Хоминг print-ове само при debug&1; ABORT винаги принтира
  - CDC командата `home` непроменена
- **Команда `range`**: измерва разстоянието между крайните изключватели
  - Кара до ES_R, изчислява raw и usable (raw − homeoff) mm
  - Записва rangeUsableMm, връща се на 0 (endstops изключени по време на връщане)
- **Команда `moveto <mm>`**: абсолютно позициониране
  - Изисква posHomed AND rangeUsableMm > 0
  - При target < 0 или > range → 100ms бип + error
  - delta = target − currentMm → Stepper_Move(delta)

### Поправено / Подобрено
- При спиране на мотора (wasBusy→idle): `\r\n\r\n` + prompt (двойна нова линия)
- Blocking команди (home, range, combo): завършват с `\r\n\r\n` + prompt
- Форматиране на промпта: `%7.2f` вместо `%8.2f` — изравнен с `XXXX.XX >`
- Bounce повторно натискане на jog след пускане: `lastTick_jog` вече се нулира при пускане, потискайки bounce-натискания за `DEBOUNCE_REL_MS` (50ms) след пускане. Засягаше и кратко натискане (JOG_STEP), и дълго задържане (JOG_CONT).

### Променено
- Въведен `DEBOUNCE_REL_MS = 50ms` (отделен от `DEBOUNCE_MS = 30ms`). Jog дебаунсът след пускане е 50ms — бутоните отскачат повече при пускане.
- Проверката на крайния изключвател при jog натискане е преместена в ISR чрез `snapA` — блокираните посоки се отхвърлят незабавно, без събитие в опашката на main loop.
- Премахнат излишният `Stepper_Stop()` от обработчика `ProcessEvents` EVT_JOGx_UP — спирането вече е гарантирано от ISR.
- `snapA = GPIOA->IDR` snapshot при влизане в ISR — всички проверки на пинове използват консистентно състояние.
- Флаг `buzzRequest`: зумерът е отложен за main loop (избягва race condition с MorseUpdate).
- NVIC приоритетите фиксирани чрез `FixNVIC_Priorities()`: TIM2 + ES крайни изключватели с приоритет 0, jog с приоритет 0, step с приоритет 2, USB с приоритет 3. Прави `Stepper_Stop()` от EXTI ISR безопасен без `__disable_irq()`.
- Edge lockout на крайни изключватели (30ms) в нормален режим — защита от EMI/вибрации.

## [e243560] — 2026-03-18 (GPIO snapshot, ISR блокиране на крайни изключватели, debug guards)

## [Непубликувано] — 2026-03-04 (branch с lookup таблица, без commit)
**Активен фърмуер — stepper.c mtime 2026-03-04 09:32**

### Променено
- Заменена формулата на Lindstedt в ISR с предварително изчислени рампови таблици
  - `BuildRampTables()` извиква се веднъж при стартиране на движение, изгражда `accelTable[]` и `decelTable[]`
  - ISR сведен до чисто търсене в таблица: `currentPeriod = accelTable[accelIndex++]`
  - FPU `vsqrt.f32` използван вътре в `BuildRampTables()` (не в ISR)
  - `MAX_RAMP_STEPS = 512` записа на таблица
- Премахнати состоянията `STEPPER_DIRSETUP` и `STEPPER_STOP`
- Запазени алиасите `movel <mm>` / `mover <mm>` за насочени движения
- Известен проблем: преходът `CONST → DECEL` задава `decelIndex = 0` (бавен край) — в процес на работа

---

## [96ec342] — 2026-03-04 09:52
### Променено
- Подобрен профил на движение за кратки движения (триъгълен профил)
- Изчисляване на пикова скорост от наличните стъпки чрез `v_peak = sqrt(2*a*d*steps/(a+d))`
- Хардуерен `VSQRT.F32` чрез inline ASM (`sqrtf_approx`) — без зависимост от libm

## [5bf1d37] — 2026-03-04 08:48
### Поправено
- Незначителни поправки в defines и код

## [0f5c05f] — 2026-03-04 08:35
### Добавено
- `stepper.c` / `stepper.h` — пълен модул за stepper драйвер
  - TIM2 CH3 на PB10 — хардуерна PWM генерация на импулси, ширина 50µs
  - Трапецовиден профил на движение: ACCEL → CONST → DECEL
  - Формула на Lindstedt за онлайн пресмятане на периода в ISR
  - Естествени единици: mm/s, mm/s², стъпки/mm
  - EEPROM запазване за всички 7 параметъра на движението
  - Структура `params_t` с union float/uint32 за безпотерно EEPROM съхранение
- CLI команди: `move <mm>`, `steps <n>`, `set <param> <f>`, `params`, `save`, `stop`
- Верифицирано с осцилоскоп и Saleae логически анализатор

## [3121724] — 2026-03-03 14:37
### Добавено
- EEPROM емулация с Flash сектори 6 и 7 (изравняване на износването)
- USB CDC RX чрез пръстеновиден буфер в callback `CDC_Receive_FS`
- VT102 парсер на клавиши: стрелки, backspace, Ctrl+L изчистване на екрана
- `printf` насочен чрез `_write()` с busy-wait на TxState + timeout 100ms
- Парсинг на команди от линеен буфер чрез `sscanf`

## [e2b05dd] — 2026-03-03 15:06
### Добавено
- Парсер на `SEQ_O` за Enter от цифрова клавиатура и клавиши F1–F4
- Вградени команди: `cls`, `help`, `uptime`, `reset`
- Парсинг на float чрез sscanf + printf float изход чрез `-u _printf_float`
### Поправено
- RX цикълът вече не блокира при TX busy-wait
- Отделени RX и TX фази

## [81166e3] — 2026-03-02 07:06
### Променено
- Премахнат междинен TX буфер (`buffx`) — използват се директно USB CDC буферите
- Регистрирани custom `MyPCD_DataInStageCallback` / `MyPCD_DataOutStageCallback`

## [01beeb1] — 2026-02-28 13:23
### Добавено
- Регистрирани USB физически connect/disconnect/suspend/SOF callbacks чрез HAL PCD callback API

## [678f918] — 2026-02-28 07:57
### Добавено
- Морзе код за цифри (0–9)
- Дефиниции `dotTime`, `dashTime`, `spaceTime`, `interTime`
- LED + зумер задвижвани заедно в `dot()` / `dash()`

## [a45ffb7] — 2026-03-02 18:03
### Поправено
- DTR беше причината за USB CDC disconnect проблема
- Решено с `hupcl` в `stty` listen скриптовете

## [fc43ba4] — 2026-02-27 13:38
### Променено
- Включени `-Werror -Wall -Wextra` — предупрежденията вече са грешки

## [b57732d] — 2026-02-26 15:32
### Променено
- Системният часовник потвърден при 96MHz (HSE 12MHz, PLL ×8)

## [3e23d3c] — 2026-02-26 15:23
### Добавено
- USB OTG FS устройство + конфигурация на системния часовник в CubeMX

## [1fe0e96] — 2026-02-26 13:53
### Добавено
- Втори commit — установена структура на проекта

## [7ec1732] — 2026-02-26 13:52
### Добавено
- Първи commit — STM32F411CEU6 HAL bootstrap с генератор на Qt Creator файлове
